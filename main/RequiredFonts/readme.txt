=== Required fonts in order to build BokuProto ===

These fonts have been added to the game as SpriteFonts. In order to build the binary XNB from 
the SpriteFont specification files, you need to have these fonts installed. 
After installing them you will need to close and relaunch your Visual Studio for the build 
process to detect the new fonts.
